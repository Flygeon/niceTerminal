niceTerminal — UI/UX 设计书 & 核心功能设计书
优化版 v2.0 | 基于架构解耦、用户体验差异化、安全性与可扩展性四个维度全面升级

第一部分：UI/UX 设计书（优化版）
1. 设计愿景
niceTerminal 的设计哲学是 “颜值与性能并存，个性与规范统一” 。它不仅仅是一个命令行入口，更是一个让开发者感到愉悦、自豪的日常工具。

1.1 核心设计原则
原则	说明
美观悦目	全面遵循 Material Design 3 设计语言，提供流畅的动效、和谐的色彩与精致的细节
清晰高效	信息层级分明，操作路径最短，让用户专注于终端内容本身
个性表达	从动态色彩到布局自定义，让每位用户都能打造属于自己的终端
轻量现代	视觉上干净通透，不堆砌冗余元素，符合现代桌面应用的审美标准
1.2 设计参考基准
参考以下产品的视觉品质标准：Warp 的温暖与克制、Linear 的精致与统一、Raycast 的流畅与高效、WezTerm 的多窗口与分屏流畅度。目标是让 niceTerminal 给人一种 “高级感” ——打开应用的第一感觉是“这很精致”。

【优化】1.3 响应式布局策略

三栏式布局在不同窗口尺寸下的自适应行为：

窗口宽度	侧边栏	右侧面板	终端主区域
≥ 1200px	展开 (200px)	展开 (240px)	完整宽度
900-1199px	展开 (180px)	折叠为图标触发器	自适应
600-899px	折叠为图标触发器	折叠为图标触发器	完整宽度
< 600px	完全隐藏（抽屉菜单）	完全隐藏	最大化
折叠触发器：当面板折叠时，在窗口边缘显示一个 32px 宽的垂直条，包含图标按钮，hover 时展开为完整面板（类似 macOS 的触控栏触发区）。

【优化】1.4 高DPI/Retina适配

终端工具对字体渲染极其敏感。niceTerminal 将：

使用 Tauri 的 window.appearance 监听系统缩放变化

针对不同 DPI 设置，渲染引擎启用 亚像素抗锯齿 (Subpixel Anti-aliasing) ，确保在高分辨率屏幕下字体清晰锐利

在 Windows 平台启用 ClearType，macOS 使用 Core Text 渲染

2. 设计系统：Material Design 3 完整规范
2.1 色彩系统 (Color System)
【优化】语义色彩扩展

在原有的红/绿/黄/蓝基础上，新增终端特定的语义色彩映射：

终端语义	ANSI 色	M3 映射	用途
正常文本	Foreground	--md-sys-color-on-surface	默认文字
错误输出	Red	--md-sys-color-error	stderr 内容
成功状态	Green	--md-sys-color-tertiary	命令执行成功
警告信息	Yellow	--md-sys-color-warning (新增令牌)	警告输出
链接/路径	Cyan	--md-sys-color-primary	可点击链接
高亮选中	Selection	--md-sys-color-primary-container (50% opacity)	文本选择
新增配色令牌（扩展 M3 标准）：

css
--md-sys-color-warning: #f9a825;      /* 警告色 */
--md-sys-color-on-warning: #000000;   /* 警告色上的文字 */
--md-sys-color-info: #0288d1;         /* 信息色 */
--md-sys-color-on-info: #ffffff;      /* 信息色上的文字 */
2.2 字体排印 (Typography)
【优化】字体回退策略：

css
/* 界面字体 - 多层回退 */
--md-sys-typescale-font-family: 'Inter', -apple-system, BlinkMacSystemFont, 
  'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;

/* 等宽字体 - 保证所有平台一致 */
--md-sys-typescale-font-family-mono: 'JetBrains Mono', 'Cascadia Code', 
  'Fira Code', 'Consolas', 'Courier New', monospace;
3. 界面布局与组件设计（优化版）
3.1 整体布局架构
【优化】新增窗口最小尺寸：主窗口最小宽度 480px，最小高度 300px。低于此尺寸时，自动折叠所有辅助面板。

3.2 组件详细设计
【优化】3.2.1 自定义标题栏 - 多窗口拖拽

支持将标签页拖出当前窗口，创建新的独立窗口

新窗口保留所有主题和配置，独立管理会话

支持将窗口中的标签页拖回原窗口

此功能基于 Tauri 的多窗口 API 实现

【优化】3.2.6 状态栏 - 信息密度调节

状态栏内容可通过设置界面自定义显示哪些模块，并提供三种预设模式：

模式	显示内容
完整模式 (默认)	工作目录 | Shell类型 | Git分支 | CPU | 内存 | 主题切换
简洁模式	工作目录 | Shell类型 | 主题切换
极简模式	Shell类型 | 主题切换
每个模块支持独立开关，用户可以自定义显示/隐藏任意状态栏组件。

4. 主题系统设计（优化版）
【优化】4.4 主题预览与即时生效

实时预览：在设置界面调整颜色时，终端区域即时响应，无需关闭设置窗口

无闪烁切换：主题切换采用 CSS 变量过渡动画 (300ms)，避免黑白闪烁

每个会话独立主题（高级模式）：允许为不同的标签页设置不同的配色方案

第二部分：核心功能设计书（优化版）
1. 终端引擎 (Terminal Engine)
1.1 架构概览（优化版）
【优化】核心架构变更：事件驱动的流式传输

text
┌─────────────────────────────────────────────────────────────────────────────┐
│                           前端 (React + TypeScript)                        │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │                         xterm.js 渲染器                             │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
│                                    ↕ (Tauri Events - 流式)                 │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │              【新增】事件流管理层 (Event Stream Manager)             │  │
│  │  • 接收 Rust 端数据流  • 分帧合并  • 渲染节流控制                  │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
│                                    ↕ (Tauri Commands - 控制指令)           │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │                       Tauri 命令层 (Rust)                           │  │
│  │  • PTY 管理  • 进程控制  • 系统交互  • 文件操作                    │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
│                                    ↕                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │              【新增】Rust 后端缓冲区 (Buffer Manager)               │  │
│  │  • 合并输出块  • 频率限制  • 敏感信息清洗  • 超大输出截断          │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
│                                    ↕ (PTY 流)                             │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │                   系统 Shell (PowerShell / Bash / Zsh)              │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
架构优化说明：

原方案问题	优化方案
Tauri Commands 请求/响应模型处理高频输出时会阻塞	采用 Tauri Events 流式推送，双向异步通信
大量输出导致 React 频繁重渲染	在 Rust 端建立缓冲区，按帧合并输出，节流推送
无输出上限保护	增加 超大输出截断机制（默认 10MB 触发）
1.2 流式数据传输协议
rust
// Rust 端 - 事件定义
#[derive(Clone, Serialize)]
pub struct TerminalOutputEvent {
    pub session_id: String,
    pub data: String,           // 当前帧数据
    pub frame_id: u64,          // 帧序号
    pub is_truncated: bool,     // 是否被截断
    pub total_bytes: u64,       // 累计输出字节数
}

// 前端监听
tauri.on('terminal:output', (event) => {
    terminal.write(event.payload.data);
});
【优化】1.3 渲染性能的防御性设计

机制	实现	触发条件
超大输出截断	Rust 端累积输出字节数，超过阈值（默认 10MB）时截断	输出 ≥ 10MB
渲染节流	前端收到事件后，合并多个帧，限制最大刷新率 60fps	连续输出时
行数限制	xterm.js 的 scrollback 选项，默认 10000 行	超过设定行数时旧行被丢弃
内存保护	每个会话最大缓冲区 50MB，超出时触发保护机制	缓冲区 ≥ 50MB
用户通知	当触发截断或保护时，状态栏显示警告图标，点击查看详情	事件触发时
2. 会话管理 (Session Management)（优化版）
【优化】2.5 会话生命周期事件追踪

每个会话记录完整的生命周期事件，便于调试和性能分析：

typescript
interface SessionLifecycle {
  id: string;
  createdAt: timestamp;
  startedAt: timestamp;
  totalOutputBytes: number;     // 累计输出
  peakFPS: number;             // 峰值渲染帧率
  avgLatency: number;          // 平均输入延迟
  crashCount: number;          // 崩溃次数
  lastCrash?: timestamp;
}
【优化】2.6 会话快照与恢复

自动快照：每 30 秒自动保存当前会话的 PTY 状态（不保存敏感数据）

崩溃恢复：会话意外崩溃时，自动尝试恢复并恢复当前工作目录

手动快照：用户可手动创建会话快照，用于备份或分享

3. 标签页系统 (Tab System)（优化版）
【优化】3.3 全局拖拽 (Global Drag)

标签页支持拖出当前窗口成为独立窗口

独立窗口完整支持所有功能（标签页、分屏、设置）

支持将标签页从独立窗口拖回主窗口

采用 Tauri 的 window.create 和跨窗口通信机制

4. 分屏系统 (Split Pane)（优化版）
【优化】4.3 分屏的全局拖拽

分屏面板支持拖拽到另一个窗口或同一窗口的其他位置

分屏面板支持拖拽合并到其他标签页

拖拽过程中显示目标位置的蓝色高亮指示器，提示放置位置

5. 主题与外观系统（优化版）
【优化】5.4 主题热重载 (Hot Reload)

修改配置文件时，应用无需重启即可应用新主题

使用 tauri-plugin-fs 监听配置文件变化

变化检测采用防抖 (debounce) 500ms，避免频繁重绘

6. 快捷键系统 (Keyboard Shortcuts)（优化版）
【优化】6.3 快捷键冲突检测与解决

机制	说明
冲突检测	当用户设置新快捷键时，自动检测是否已被其他功能占用
冲突提示	UI 显示“该快捷键已被 [功能名] 占用”，并提供“覆盖”或“取消”选项
优先级策略	优先响应用户自定义的映射，其次是系统默认，最后是应用内置
冲突视图	设置界面中，冲突的快捷键用红色高亮显示
【优化】6.4 模态快捷键策略

当命令面板、设置界面、对话框等模态组件打开时：

全局快捷键（如 Ctrl+T 新建标签页）被暂停

模态组件内部的快捷键（如 Ctrl+F 搜索）生效

关闭模态后，全局快捷键自动恢复

7. 终端内交互增强（优化版）
【优化】7.4 命令建议 (Command Suggestions)

基于用户历史命令，提供智能补全建议（Phase 2）

建议显示为下拉列表，键盘上下选择，Tab/Enter 补全

支持自定义补全规则（用户可添加常用命令模板）

【优化】7.5 快速操作工具栏

在终端右上角（可隐藏）提供悬浮工具栏：

📋 复制

📥 粘贴

🔍 查找

🗑️ 清屏

⏹️ 中断当前命令 (Ctrl+C)

8. 配置管理 (Configuration)（优化版）
【优化】8.4 配置文件热重载 (Hot Reload)

niceTerminal 支持配置文件 (~/.niceterminal/config.json) 的热重载：

json
{
  "settings": {
    "hotReload": true  // 默认启用
  }
}
实现机制：

使用 tauri-plugin-fs 监听配置文件变化

变化检测防抖 500ms

检测到变化后，验证 JSON 格式有效性

验证通过后，增量应用新配置（不重启应用）

验证失败时，在状态栏显示错误提示，保留原有配置

适用配置项（热重载支持）：

主题/颜色

字体/字号

快捷键（需重新注册）

状态栏显示选项

不适用热重载的配置项（需重启）：

PTY 相关设置（如默认 Shell 路径）

窗口初始尺寸

9. 安全性设计 (Security)（新增章节）
【优化】新增第九章：安全架构

9.1 敏感命令确认机制
当用户尝试执行高风险命令时，niceTerminal 弹出二次确认对话框：

命令模式	触发条件	确认方式
rm -rf / 或 rm -rf /*	递归删除根目录	强制三次确认 + 输入 YES
sudo 命令	以 root 权限执行	单次确认对话框
:(){ :|:& };: (fork bomb)	检测到 fork 炸弹模式	警告 + 确认
dd if=/dev/zero of=/dev/sda	覆盖磁盘设备	警告 + 确认
技术实现：

Rust 端在将输入写入 PTY 之前，进行命令模式匹配

匹配命中时，拦截写入，向 UI 发送确认请求

用户确认后，Rust 端再执行写入

9.2 凭证管理 (Credential Management)
凭证类型	存储方式	说明
SSH 私钥	不存储	仅读取用户 ~/.ssh/ 目录，不缓存
SSH 密码	系统密钥环	Windows: Credential Manager, macOS: Keychain, Linux: Secret Service
会话 Token	加密内存	内存中加密存储，退出时销毁
命令历史	加密存储	历史命令在存储前进行 AES-256 加密
9.3 输入/输出清洗
敏感信息过滤：在日志输出中自动模糊化密码、Token、API Key 等信息

模式匹配：支持正则表达式定义敏感模式（用户可自定义）

输出净化：Rust 端缓冲区在输出到前端前进行清洗

10. 可扩展性设计 (Extensibility)（新增章节）
【优化】新增第十章：插件系统架构（Phase 1 设计，Phase 2 实现）

10.1 插件 API 钩子 (Hooks) - Phase 1 定义
在 v1.0 阶段即定义完整的插件 API 接口，确保 v2.0 插件系统无需重构：

typescript
// 插件接口定义 (v1.0 预先设计)
interface TerminalPlugin {
  id: string;
  name: string;
  version: string;
  
  // 生命周期钩子
  onActivate?: () => void;
  onDeactivate?: () => void;
  onSessionCreated?: (session: Session) => void;
  onSessionClosed?: (sessionId: string) => void;
  onOutput?: (sessionId: string, data: string) => string;  // 可修改输出
  onCommandExecuted?: (sessionId: string, command: string) => void;
  
  // UI 扩展点
  statusBarItems?: StatusBarItem[];
  sidebarItems?: SidebarItem[];
  commandPaletteCommands?: Command[];
  settingsPanels?: SettingsPanel[];
}
10.2 插件安装方式
方式	说明
本地安装	将插件文件夹放入 ~/.niceterminal/plugins/
NPM 安装	支持从 npm 注册表安装 (Phase 2)
插件市场	内置插件浏览器 (Phase 3)
10.3 插件隔离与安全
每个插件运行在独立的 Web Worker 中

插件 API 权限受限（需用户授权才能访问文件系统、网络等）

插件崩溃不影响主应用

11. 风险预估与缓解 (Risk Management)（新增章节）
【优化】新增第十一章

潜在风险	影响程度	缓解方案
高频输出卡顿	高	Rust 端缓冲区 + 渲染节流 (60fps) + WebGL 加速
IPC 通道阻塞	高	采用事件驱动的流式传输，避免请求/响应模型
快捷键冲突	中	提供快捷键冲突检测机制，优先响应本地配置
内存泄露	高	严格的 PTY 生命周期管理 + 每个会话 50MB 内存上限保护
配置文件损坏	中	配置文件加载失败时，自动回退到默认配置 + 错误日志
恶意命令执行	高	敏感命令确认机制 + 输入清洗
插件安全漏洞	中	插件隔离沙箱 + 权限申请机制
跨平台兼容性	中	针对 Windows/macOS/Linux 分别测试，利用 Tauri 抽象层
12. 功能路线图 (Roadmap)（优化版）
Phase 1: 核心终端 (v1.0)
✅ 基础终端渲染 (xterm.js + portable-pty)

✅ 事件驱动的流式传输架构

✅ 多标签页管理

✅ M3 主题系统 (8 套预设 + 自定义)

✅ 深色/浅色模式

✅ 基础快捷键 + 冲突检测

✅ 复制/粘贴/全选

✅ 终端内查找

✅ 侧边栏 (会话列表)

✅ 状态栏 (信息密度可调)

✅ 设置界面

✅ 配置文件热重载

✅ 插件 API 钩子定义 (接口先行)

✅ 敏感命令确认机制

✅ 超大输出截断与内存保护

Phase 2: 效率增强 (v1.5)
分屏系统 (左右/上下) + 全局拖拽

标签页拖出独立窗口

SSH 远程连接 + 系统密钥环集成

命令历史持久化与搜索 (加密存储)

终端内链接识别

会话恢复与工作区保存

文件树侧边栏

命令建议 (基于历史)

插件系统基础实现 (加载、生命周期)

Phase 3: 高级功能 (v2.0)
插件市场

配置云同步

AI 命令建议

性能监控面板

串口调试支持

主题社区分享

13. 技术实现要点（优化版）
13.1 项目结构（优化版）
text
niceTerminal/
├── src/                          # 前端源码 (React + TypeScript)
│   ├── components/               # UI 组件
│   ├── hooks/                    # React Hooks
│   ├── stores/                   # Zustand 状态管理
│   ├── styles/                   # CSS 变量与主题
│   ├── services/                 # 【新增】服务层
│   │   ├── eventStream.ts       # 事件流管理
│   │   ├── themeService.ts      # 主题服务
│   │   └── configWatcher.ts     # 配置热重载监听
│   └── plugins/                  # 【新增】插件 API
│       ├── api.ts               # 插件接口定义
│       └── loader.ts            # 插件加载器
├── src-tauri/                    # 后端源码 (Rust)
│   ├── src/
│   │   ├── pty/                 # PTY 管理
│   │   │   ├── mod.rs
│   │   │   ├── session.rs       # 会话管理
│   │   │   └── buffer.rs        # 【新增】缓冲区管理
│   │   ├── events/              # 【新增】事件流
│   │   │   ├── mod.rs
│   │   │   └── emitter.rs       # 事件发射器
│   │   ├── security/            # 【新增】安全模块
│   │   │   ├── commandGuard.rs  # 敏感命令检测
│   │   │   └── credential.rs    # 凭证管理
│   │   ├── commands/            # Tauri 命令
│   │   └── utils/
│   └── Cargo.toml
└── tauri.conf.json
13.2 关键依赖（优化版）
依赖	版本	用途
tauri	v2	应用框架
tauri-plugin-events	v2	事件流式传输
tauri-plugin-store	v2	配置存储
tauri-plugin-fs	v2	配置热重载监听
portable-pty	0.8+	Rust PTY 管理
tokio	1.x	异步运行时
serde / serde_json	1.x	序列化
aes-gcm	0.10+	命令历史加密
keyring	3.x	系统密钥环集成
13.3 关键性能指标 (KPI)
指标	目标值	测量方式
应用冷启动时间	≤ 500ms	Tauri 启动测量
终端输入延迟	≤ 50ms (P99)	从按键到屏幕响应
滚动帧率	≥ 60fps	大量输出滚动时
内存占用 (空闲)	≤ 150MB	任务管理器
内存占用 (重度)	≤ 500MB	连续运行 8 小时 + 大文件输出
输出吞吐量	≥ 10MB/s	文本流写入速度
14. 总结
niceTerminal 项目通过本次优化，在原有“Tauri + Material Design 3”的坚实基础上，实现了以下关键升级：

架构解耦：引入事件驱动的流式传输 + Rust 端缓冲区，从根本上解决了高频输出场景下的性能瓶颈

安全性加固：敏感命令确认、系统密钥环集成、输入/输出清洗、命令历史加密——让终端工具安全可靠

差异化体验：全局拖拽、响应式布局、信息密度调节、高DPI适配——在细节处超越竞品

可扩展性保障：Phase 1 定义插件 API 钩子，确保 v2.0 插件系统无痛接入

这是一份具备工业级落地潜力的技术方案，随时可以进入开发阶段。

本文档版本 2.0 (优化版) | 最后更新 2026年8月